<div class="container mt-3">
  <h1>Add a new cocktéléphant 🐘</h1>

  <%= simple_form_for @cocktail do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "add a cocktail", class: "btn btn-primary" %>
  <% end %>
</div>
